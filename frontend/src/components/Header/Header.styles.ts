import { iconButtonClasses } from "@mui/material";

const styles = {
    input: {
        "& fieldset legend": {
            display: "none",
        },
    },
    iconButton: {
        color: "white",
        "&:hover": {
            color: "black",
        },
    },
};

export default styles;
